# NutritionPlan: Project Notes

> Living document. Read this first at the start of every new chat to get context.
> Update it whenever something meaningful changes (decisions, features, setup, open issues).

**Last updated:** 2026-09-16

---

## 1. Project Overview
- **What:** A personal, single-user Streamlit app for a doctor. The doctor photographs handwritten patient notes, and the app stores them per patient in their own Google Drive. An AI transcribes the notes into `Info.md` and on request only, generates a nutrition plan (`NutritionPlan.md`) and a basic exercise plan (`ExercisePlan.md`), each overwritten per request, from the notes, the doctor's typed remarks and the saved plan. It also gives homeopathic medicine recommendations (`MedicineListN.md`) that the doctor can revise with their own input.
- **User:** only the doctor, who is in India (so `APP_TIMEZONE` stays at its default `Asia/Kolkata`). No multi-user features, sharing or deployment; it runs locally.
- **Location:** `/home/yuvraj/projects/NutritionPlan` (WSL2 / Linux)
- **Original spec:** the "Build Prompt: Personal Patient Case-Study & Nutrition Plan App" given in the first build chat (2026-09-15). The wireframe `NutritionPlanInterface.png` it mentions has **not** been added to the project.

## 2. Tech Stack
| Layer    | Choice | Notes |
|----------|--------|-------|
| UI       | Streamlit (Python 3.12), 1.63 installed | Screens controlled with `st.session_state.screen` |
| Storage  | Google Drive API v3 | Only storage. Scope: full `drive` |
| Auth     | Google OAuth 2.0, Desktop-app client | `credentials.json` plus cached `token.json` |
| AI       | Claude by default (`claude-opus-5`); Gemini/OpenAI selectable | Set with `AI_PROVIDER` in `.env` |
| Env      | `.venv` created with `uv` | `uv pip install --python .venv/bin/python -r requirements.txt` |

## 3. Project Structure
```
NutritionPlan/
├── app.py              # Streamlit UI: screens, workflows (create/update/plan)
├── drive.py            # OAuth login/token cache + Drive helpers (Drive class)
├── ai.py               # transcribe_notes(), generate_plan(), provider adapters
├── config.py           # reads .env
├── pdf.py              # Markdown → PDF for downloads (fpdf2 + markdown)
├── style.css           # app look (cards, charcoal tiles, pills); injected by app.load_styles()
├── .streamlit/config.toml # theme colors + Poppins font (committed; secrets.toml stays ignored)
├── chat.py             # records chat: in-memory BM25 index over NutritionPlan/ + context selection
├── fonts/              # DejaVu Sans regular/bold + license, used by pdf.py
├── prompts/
│   ├── info_system.md  # transcription instructions
│   ├── plan_system.md  # plan-writing instructions ({{PLAN_TEMPLATE}} placeholder)
│   ├── plan_template.md# plan sections (doctor can edit)
│   ├── exercise_system.md   # exercise-plan instructions
│   ├── exercise_template.md # exercise-plan sections (doctor can edit)
│   ├── chat_system.md       # records chat instructions
│   ├── medicine_system.md   # homeopathic recommendation instructions
│   └── medicine_template.md # medicine-list sections (doctor can edit)
├── requirements.txt
├── .env.example        # copy to .env
├── .gitignore          # ignores .env, credentials.json, token.json, .venv
├── README.md           # setup: Drive API, OAuth, keys, run
└── Note.md
```

Drive layout (must match exactly): `NutritionPlan/<Patient>/Documents/{Photos/, Info.md, Extra_info.md}` `NutritionPlan/<Patient>/Nutrition_Plan/NutritionPlan.md`, `NutritionPlan/<Patient>/Exercise_Plan/ExercisePlan.md` and `NutritionPlan/<Patient>/Homeopathic_Medicine/MedicineListN.md`.

## 4. Setup & Run
See `README.md`. In short:
1. `cp .env.example .env` and set `ANTHROPIC_API_KEY`.
2. In Google Cloud: enable the Drive API, set up the OAuth consent screen (External, Testing, **add own Gmail as a test user**), create an OAuth client of type **Desktop app**, and save it as `credentials.json`.
3. `source .venv/bin/activate && streamlit run app.py`

## 5. Features
### Done (2026-09-15)
- Screen 1 Login: Google OAuth with a cached token, skipped automatically when the token is valid; creates `NutritionPlan/`.
- Screen 2 Search: case-insensitive exact match opens the patient; partial or duplicate matches show a pick-list with a "create new" option; no match goes to New Case Study.
- Screen 3 Patient options: Download Case files (per file plus ZIP; since 2026-09-16 only case study, nutrition/exercise plan, latest medicine list and photos), Open Case Study (Info, Extra info, photo gallery, plan dropdown), Update Case Study, Request Diet Plan, Back.
- Screen 4 New Case Study: uploader and camera, observations; creates the tree, uploads photos, writes Info.md and Extra_info.md (no plan since 2026-09-16).
- Screen 5 Update: add photos (default) or replace them (with a confirmation checkbox), append dated observations; regenerates Info.md when photos change; no plan generated.
- Plan generation: sends Info.md (photos only if Info.md is missing), Extra_info.md and the latest plan with the "Review this previous plan…" instruction; (since 2026-09-16: overwrites `NutritionPlan.md`; skipped when nothing changed).
- Spinners/status for uploads and AI calls; Drive and AI errors shown on the page instead of crashing.

### Done (2026-09-16)
- **Exercise plan**: "🏃 Request Exercise Plan" button on the patient screen. Uses Info.md (photos if missing), Extra_info.md and the latest exercise plan; saves `Exercise_Plan/ExercisePlanN.md` (never overwrites). Shown with a version dropdown under "Exercise plan" in Open Case Study; included in downloads/ZIP. Not auto-generated for new patients.
- **Homeopathic medicines**: "💊 Homeopathic Medicines" button opens the `medicine` screen. Output is a Recommendation/Potency/Rate table only. Step 1: optional extra input + "Generate recommendations" (Info.md + Extra_info.md, fresh; does not use earlier lists). Step 2 (shown once a list is on screen): "Doctor's recommendations" + "Create revised list" → the AI revises the list on screen, applying the doctor's changes first. Every list is saved as the next `MedicineListN.md` (header notes "revised from MedicineListM.md"; the doctor's input is quoted under `## Doctor's Input`). Revisions can repeat. Listed in Open Case Study and included in downloads.

- **Records chat** (search screen): a floating "💬 Ask about patients" button slides up a chat panel (fixed bottom-right, CSS on keyed containers `st-key-chat-launcher` / `st-key-chat-panel`; the slide-up animation plays only on the opening run). The doctor can ask about any patient or about all records. **Search is done locally, not by the AI**: `chat.Index` (one per process, `st.cache_resource`) indexes every `.md`/`.txt` under `NutritionPlan/`, split into sections of at most 1,500 chars at headings or paragraphs, ranked with BM25 and a light stemmer. Patients are matched by full name or a name word unique to one patient (typos allowed with difflib ≥ 0.85). Context per question: the patient roster, plus the named patients' current documents in full (≤ 3 patients, budget 48k chars ≈ 12k tokens), plus up to 20 excerpts ranked by BM25 (×2 for named patients). Superseded versions (older `MedicineListN`, legacy `PlanN`/`ExercisePlanN`) are skipped unless the question has a history word ("previous", "changed", "history"…). Follow-ups with no name keep the previous patients. History: the last 6 turns, answers cut to 1,500 chars. Each answer lists "Records used" with an approximate token count.
- **PDF downloads**: every Markdown document is offered as PDF (plan/medicine download buttons, Download Case files, ZIP). Drive still stores `.md`. Photos unchanged.

### Planned / Backlog
- _Nothing agreed yet._ Possible ideas: a button to regenerate Info.md on its own; HEIC photo support.

## 6. Key Decisions
| Date       | Decision | Reason |
|------------|----------|--------|
| 2026-09-15 | Keep project notes in `Note.md` | Carry context between chat sessions |
| 2026-09-15 | AI provider default **Claude `claude-opus-5`**; Gemini (`google-genai`) and OpenAI adapters included | Spec left provider as `[DECIDE]` and required it to be swappable |
| 2026-09-15 | Claude calls use streaming plus server-side refusal fallback (`fallbacks: "default"`, beta `server-side-fallback-2026-07-01`) | A safety decline is retried on a fallback model instead of failing |
| 2026-09-15 | Plan template = the spec's suggested sections, in `prompts/plan_template.md` | Spec left it as `[DECIDE: adjust to your practice]`; the doctor can edit it without touching code |
| 2026-09-15 | Full `drive` OAuth scope (not `drive.file`) | Lets the app see folders or files the doctor creates or moves manually in Drive |
| 2026-09-15 | Replaced photos are moved to **Drive trash**, not permanently deleted; new photos are uploaded *before* old ones are trashed | Safer destructive action (recoverable for 30 days) |
| 2026-09-15 | Uploaded photo names get a `YYYYMMDD-HHMMSS_NN_` prefix | Avoids name collisions (camera shots are all "image.jpg") and keeps them in chronological order |
| 2026-09-15 | Photos sent to the AI are EXIF-rotated, downscaled to 2400px and re-encoded as JPEG q90; originals stored untouched | Stays under provider image limits while keeping handwriting legible |
| 2026-09-15 | Plan generation uses Info.md whenever it exists | Info.md is always regenerated when photos change, so it is current; avoids re-sending every photo |
| 2026-09-15 | "Add" mode regenerates Info.md from **all** photos (existing + new) | Keeps one coherent transcription and summary |
| 2026-09-16 | Exercise plans live in a separate `Exercise_Plan/` folder named `ExercisePlanN.md`, with their own prompt/template (`prompts/exercise_*.md`) | Independent versioning from nutrition plans; distinct file names so individual downloads don't clash with `PlanN.md` |
| 2026-09-16 | **AI is called only when the doctor asks** (Request Diet/Exercise Plan, Generate/Revise medicines) or when photos are added/replaced (Info.md transcription). No auto Plan1 on patient creation. Notes-only updates make no AI call | User requirement |
| 2026-09-16 | Diet and exercise plans are **one file each, overwritten** (`NutritionPlan.md`, `ExercisePlan.md`); the saved plan is still sent as "previous plan". Medicine lists stay versioned | User requirement (overwrite was asked for diet and exercise only). Drive's own version history keeps overwritten versions |
| 2026-09-16 | **Skip the AI when nothing changed**: a request compares Drive `modifiedTime` of Info.md, Extra_info.md and photos with the saved output; if none is newer, the saved output is shown ("Regenerate anyway" / "Generate new recommendations anyway" force a call). Medicine extra input always counts as new | User: no update unless something was added. Metadata-only check, no file reads until needed |
| 2026-09-16 | Old numbered plans are left in Drive; the newest is shown and used as the previous plan until the single file exists | No data deleted; only the test patient "yuvraj" had them |
| 2026-09-16 | Medicine recommendations: every generated or revised list is a new `MedicineListN.md` with the doctor's input quoted in it; "Generate" starts fresh, and only "Create revised list" builds on an earlier list | Keeps a record of every revision and who asked for what; matches the two-step flow the user described |
| 2026-09-16 | Medicine prompt: follow the doctor's instructions even when concerned (concern goes under Cautions); never advise stopping conventional medication; point out red flags | The doctor is the prescriber; patient safety |
| 2026-09-16 | Gemini calls retry up to 3 times on 5xx `ServerError` (waits 3 s, then 8 s); 4xx errors are not retried | A real test hit `503 UNAVAILABLE` ("high demand") |
| 2026-09-16 | Exercise plan is only generated on request (not with Plan1 on patient creation) | The user asked for an extra option; avoids an extra AI call per new patient |
| 2026-09-16 | Records chat: **the app selects the records (local BM25 + name matching); the AI only answers from what it is given**, with no tool or agentic file search | User requirement: don't spend tokens on finding files |
| 2026-09-16 | Chat index is in memory only (not saved to local disk). Drive is re-listed at most every 60 s (sooner after the app writes), with `Drive.walk_tree` (batched `'a' in parents or …` queries level by level). Only new or changed files (by `modifiedTime`) are downloaded, 8 in parallel through per-thread connections (`Drive.download_many`) | Patient data stays only in Drive; cheap refreshes. Real Drive: 18 docs first build 6 s (21 s sequential), unchanged refresh 1.9 s |
| 2026-09-16 | Patient code = 3 capital letters of the name + lowest free 2-digit number; written to tab 2 of `Appoinment_history` before the patient folders are created | User spec; no AI needed; failing first avoids patients without a code |
| 2026-09-15 | App writes a header line on Info.md and each plan (timestamp, provider/model, "AI draft for clinician review") | Traceability |

## 7. Open Questions / Issues
- PDFs use DejaVu Sans, so emoji and Indic scripts (e.g. Devanagari) are dropped from PDFs (fpdf2 logs a missing-glyph warning; no crash). Fix if needed: bundle Noto Sans Devanagari as a fallback font plus `uharfbuzz` for text shaping. Italic text prints upright (no oblique face bundled).
- Not yet tested against real Google Drive or a real AI key; only smoke-tested with an in-memory fake Drive and a mocked AI (all checks passed). First real run still needed.
- Gemini/OpenAI default model names (`gemini-2.5-pro`, `gpt-5`) and adapters are untested; check them before switching providers.
- The user mentioned an earlier authentication failure; details unknown. README has a troubleshooting table (most likely causes: missing test user → 403 access_denied; Web client instead of Desktop → redirect_uri_mismatch; Testing-mode tokens expire after 7 days).
- WSL: if the browser doesn't open on Login, copy the sign-in URL printed in the Streamlit terminal.
- Privacy: patient data goes to Google Drive and the AI provider. The doctor should confirm this meets patient-consent and data-protection rules.

## 8. Changelog
- **2026-09-16**: **Manage my Patients** screen (`manage`). A "Manage my Patients" button sits under the search form on the home (search) screen. The new screen has "Back to search" and a **List all my patients** button, which reads tab 1 of `Appoinment_history` (`Drive.visit_counts`), groups rows by PatientName ignoring case (shows the spelling seen first) and shows a Patient / Visits / Latest visit (`DD Mon YYYY, HH:MM`, latest by date then time; `drive.PatientVisits`) table, most visits first, then by name, with a "N patients, M visits in total" caption. Only patients with at least one visit appear. State `patient_counts` (cleared on entering or leaving the screen; re-read on every click). Real sheet: yash = 2. Browser-tested with a fake sheet at 1280px and 390px (case-insensitive grouping, back/reopen clears the list, chat button not shown there). Server restarted.
- **2026-09-16**: **Appointment History** button next to Open Case Study / Download Case files (view `history`). It looks up the patient's code in tab 2 by name, then shows every row in tab 1 **with that PatientCode** as a table of Date (`DD Mon YYYY`) and Time only (PatientCode/PatientName columns removed at the user's request), newest first, with a count caption. No code → "Clock in a visit to give them one"; code but no rows → "No visits recorded for <code> yet". Cached per patient as `visits`; clicking the button re-reads the sheet, and a saved visit clears the cache. `drive.py`: `Visit` dataclass, `_find_code` (now shared with `record_visit`), `patient_visits`. Real sheet read-only: yash → YAS01, 3 visits sorted correctly; yuvraj → no code. Browser-tested with a fake sheet (no code, clocking in refreshes the table, code without visits). Server restarted.
- **2026-09-16**: **Confirmation for a second visit on the same day** (user request). Before saving (both "Add current time" and a manual date), `Drive.visit_times(root, name, day)` reads tab 1 (`UNFORMATTED_VALUE` + `SERIAL_NUMBER`, parsed by `_cell_date` / `_cell_time`, which also accept hand-typed `YYYY-MM-DD`, `DD/MM/YYYY`, `DD-MM-YYYY`) and matches rows by patient name (case-insensitive). If there are visits that day, the dialog replaces its options with a warning ("yash is already clocked in today (1 visit at 23:17). Add another visit at 23:25?") and **Yes, add another visit** / **Cancel** (Cancel goes back to the options). State `visit_pending`, reset when the dialog opens. `app.py`: `check_and_save_visit`, `confirm_repeat_visit`. Browser-tested with a fake sheet (first visit no prompt, repeat prompts, cancel, close/reopen clean, confirm saves, another day no prompt, a repeat on a manual date names that date). Real sheet read-only: parsing correct. **The real sheet has a future row: yash 2026-09-17 23:12** (added before the date limit existed); left for the user to delete.
- **2026-09-16**: **Clock-in visits can't be in the future** (user request). "Add manually": the Date picker has `max_value` = today (in `APP_TIMEZONE`), and a typed future date snaps back to today. `st.time_input` has no max, so "Save visit" checks date+time against the current time when clicked and shows "A visit can't be in the future…" without saving (the dialog stays open). Browser-tested with a fake sheet: a later time today is blocked; tomorrow is greyed out in the calendar; typed tomorrow was saved as today; an earlier time today saves.
- **2026-09-16**: **Clock in Visit** button beside the patient name (patient screen; stacks under the name on phones). It opens a dialog (`st.dialog`) with "Add current time" (saves right away) and "Add manually" (shows Date DD/MM/YYYY + Time, then "Save visit"). Each visit appends `[PatientCode, PatientName, YYYY-MM-DD, HH:MM]` to the **first tab** of `Appoinment_history` with `USER_ENTERED`, so Sheets stores real dates/times. The code is looked up in tab 2 by name (case-insensitive); patients without one (created before codes, e.g. "yuvraj") get a new code there first. "Current time" uses the new `APP_TIMEZONE` setting (default `Asia/Kolkata`) because the server runs in UTC. `drive.py`: `_appointment_tabs`, `_read_rows`, `_append_row`, `record_visit`; `register_patient_code` shares them. `app.py`: `local_now`, `save_visit`, `clock_in_dialog`, state `visit_manual`. `style.css`: `.st-key-clockin`. Tested in headless Chromium with a fake sheet at 1280px and 390px: both paths, flash message, dialog closes, reopening hides the manual fields, existing code reused. The Sheets API is now enabled and works (patient "yash" got `YAS01`). Not yet tried against the real sheet from the app. Note: Escape closes the whole dialog. Not committed.
- **2026-09-16**: **Patient codes in `Appoinment_history`** (Google Sheet in `NutritionPlan/`, name spelled that way in Drive; tab 1 = PatientCode/PatientName/Date/Time, tab 2 = PatientCode/PatientName). On "Create patient", code (no AI) gives the patient a code: the first 3 letters of the name in capitals (padded with X if the name has fewer) plus the lowest unused 2-digit number for that prefix (`YUV01`, `YUV02`…; error once 99 are used). Then `[code, name]` is appended to the **second tab** (found by position, not title). `drive.py`: `next_patient_code()`, `Drive.register_patient_code()`, a Sheets v4 client (`Drive._sheets`; the existing full `drive` scope is enough, no new login). `app.py`: `create_patient` registers the code **before** creating folders (a failure leaves nothing half-created) and returns `(folders, code)`; the success message shows the code. Tested with a fake Sheets service. Needed the Google Sheets API enabled in Cloud project `nutritionplan-508807` (number 482696413550); now enabled. Existing patient "yuvraj" has no code. Not committed.
- **2026-09-16**: **Download Case files trimmed** (user request). It now offers only `CaseStudy.pdf` (from Info.md), `NutritionPlan.pdf`, `ExercisePlan.pdf`, `MedicineList.pdf` (the **latest** MedicineListN only) and the case photos (`Photos/…`, original format). The ZIP has the same set under `<Patient>/`. No longer included: Extra_info, older medicine lists, legacy numbered plans and any other stray files. `app.py`: new `download_documents(listing)`; `render_downloads()` builds from `case_listing()` instead of `Drive.walk()` (so fewer Drive calls). Documents that don't exist yet are skipped. Tested in headless Chromium on a fake Drive (with 2 medicine lists, Extra_info and a photo): button list and ZIP contents are exactly those 5 files, all PDFs valid.
- **2026-09-16**: Tried Times New Roman (with Tinos as the fallback) for the whole app; the user preferred **Poppins**, so the font and heading letter-spacing were reverted to the committed Poppins theme.
- **2026-09-16**: **New look, based on a smart-home app mockup the user shared** (cream background, beige cards, charcoal tiles with gold icons, black pill buttons, Poppins). `.streamlit/config.toml` sets the theme (cream `#F4EEE8`, beige `#E6DCCF`, gold `#C8964C`, Poppins from Google Fonts). `style.css` holds the layout CSS. It targets Streamlit test ids and keyed containers: `st-key-tiles`, `st-key-topbar`, and `st-key-card-*` for every bordered container. `app.py`: `load_styles()`, `hero()` (pill label + big title + muted line), `welcome_card()` (tagline + decorative bars on the password/login screens), `back_button()` (arrow link at the top). The patient screen now has 4 dark tiles (Diet Plan, Exercise Plan, Medicines, Update Case) plus Open Case Study / Download Case files pills (the one that's open is dark), and "Back to search" sits at the top. The New, Update and Medicine screens use the top back link; Update no longer has a Cancel button (the back link does the same). Emoji in buttons were replaced with Material icons. **Button labels changed** ("Request Diet Plan" → "Diet Plan", etc.), so old AppTest suites that click by label need updating. The chat panel uses the theme colors. Checked with headless Chromium against a fake Drive (Playwright in the scratchpad; libnss3/libnspr4/libasound extracted locally because there's no sudo): screenshots of every screen at 1280px and 390px, plus a click-through (search → patient → Diet Plan tile shows the saved plan with no AI call → downloads → medicines → update → back → chat open/close), all passing. Committed and pushed to `origin/main`.
- **2026-09-16**: **GitHub push from the terminal set up.** Pushes from Claude's shell had failed (no credentials; VS Code's askpass variables aren't passed to it). Installed GitHub CLI 2.101.0 to `~/.local/bin/gh` without sudo (official release, checksum verified), logged in with `gh auth login --web` (device code) as `yuvrajkhelkar01` (token saved in plain text in `~/.config/gh/hosts.yml`; scopes repo, read:org, gist), then ran `gh auth setup-git`. Pushed `a629cb3` "Add patient records chat and PDF downloads" to `origin/main`. The user should reboot the Streamlit Cloud app (new packages fpdf2, markdown).
- **2026-09-16**: **Bug fix: "'Drive' object has no attribute 'walk_tree'"** on the first chat question. Cause: `drive.py` was edited while the app was running; Streamlit reloaded the module, but the browser session still held a `Drive` built from the old class. `main()` now drops a session client that isn't an instance of the current `drive.Drive`, and the login screen reconnects from the saved token without a prompt. `records_index()` rebuilds the cached `chat.Index` the same way after `chat.py` changes. Tested with AppTest (old client → reconnected, search screen, no error; chat flow still passes).
- **2026-09-16**: **Records chat on the search screen.** New `chat.py` (Index: refresh / search / find_patients / build_context), `prompts/chat_system.md`, `ai.answer_question(question, records_md, history)` (reuses `_complete`, so it works with every provider). `drive.py`: `_list_all()` (pagination, shared with `list_children`), `walk_tree()`, `download_many()`, and `_execute(..., locked=False)` for per-thread connections. `app.py`: state `chat_open` / `chat_animate` / `chat_turns` / `chat_patients`, `records_index()`, `records_chat()`, `render_chat_turn()`, `ask_records()`. `invalidate_patient_cache()` and `create_patient()` mark the index stale. Tests: retrieval suite on a fake Drive (TTL, only changed files re-downloaded, name typo, follow-up carry-over, superseded versions, history questions) and an AppTest flow (open, ask, follow-up gets history, New chat, close, AI error shown and not saved). Real Drive (read-only): `walk_tree` returns the same 19 files as `walk`; "summary of <patient>" = 5 full docs ≈ 4.4k tokens. **Not yet checked in a real browser** (no browser in WSL): panel position, animation, mobile width. No real AI call made. Not committed.
- **2026-09-16**: **Downloads are PDF instead of .md.** New `pdf.py`: `markdown_to_pdf(text, title)` converts with `markdown` (tables, sane_lists) → HTML → fpdf2 `write_html`. It uses the bundled DejaVu Sans, A4, page-number footer. `_normalize_markdown` re-indents 2-space nested lists to 4 and inserts blank lines before lists/tables that directly follow text, so Gemini's Markdown renders properly. A custom `HTML2FPDF` subclass resets the font before each `<li>` (otherwise list markers used the previous heading's font), and table cells are left-aligned (otherwise justified). `app.py`: `markdown_pdf` (`st.cache_data`), `as_download()`, `pdf_download_button()`; case-file downloads and the ZIP contain PDFs. `requirements.txt`: fpdf2 2.8.8, markdown 3.10.3 (pure Python; checked with a clean 3.12 install). Tests: new PDF suite 11/11; earlier suites updated for `.pdf` labels (30/30, 17/17, 14/14, quota 9/9, smoke). Real Drive "yuvraj": 19 documents converted, no errors, no AI calls. Checked the exercise and medicine PDFs visually. Not committed yet.
- **2026-09-16**: **Final testing before push (Gemini quota exhausted, so no real AI calls).** Updated the old fake-Drive suites to single overwritten plan files and re-ran them: no-call 30/30, medicine 17/17, edge 14/14, smoke pass. New quota suite (fake Drive, every AI function raises a 429): 9/9. It covers create with photo, notes-only update, photo update ("Photos were saved, but Info.md could not be regenerated"), and diet and medicine requests where nothing is saved. Real Drive test with Gemini `generate_content` patched to raise `ClientError 429` (no request sent): 9/9. Search, open case study and downloads work. Diet/exercise/medicine requests on "yuvraj" each made 1 blocked attempt, because `Extra_info.md` (12:09) is newer than the saved outputs. The error is shown, the doctor's revision input is kept, 429 is not retried, and nothing is written to Drive. **Bug fixed:** after a failed create (e.g. transcription 429), the "Open patient page" button did nothing: it was drawn only in the run where Create was clicked, so its click was lost on rerun. It is now drawn on every run while the half-created patient is set, and "Back to search" on the New screen clears `patient`. Compile check and a headless `streamlit run` health check passed. Committed and pushed.
- **2026-09-16**: **AI only on request + overwrite plans.** `drive.py`: `DriveFile.modified` (Drive `modifiedTime`), `NUTRITION_PLAN_FILE` / `EXERCISE_PLAN_FILE`, `LEGACY_*_PREFIX`; `list_plans`/`save_new_plan` now need an explicit prefix (only medicine lists use them). `ai.generate_plan` takes the previous plan as text. `app.py`: `PlanKind(key, title, folder, file, legacy_prefix)`, `current_plan_file()`, `case_changed_since()`, `request_plan(kind, force)` (overwrite), `request_medicines(extra, force)` / `revise_medicines()`, `run_plan_request()` / `run_medicine_request()`; `last_plan` = (kind, text, generated), `medicine_list` = (n, text, generated); "Regenerate anyway" buttons; `screen_new` no longer makes a plan. `plan_template.md` changes wording ("when a previous plan is provided"). Tests: a new fake-Drive suite that counts every AI call, 30/30 (create, browsing, repeats with no change, regenerate, notes/photo changes, medicines incl. extra input and revisions, AI failure, legacy numbered plans). Real Drive: diet/exercise/medicine requests on "yuvraj" made **0** AI calls; Regenerate exercise made 1 call and wrote `ExercisePlan.md`; the next request made 0 calls. Old `Plan1–3.md` / `ExercisePlan1–3.md` are still in Drive for "yuvraj".
- **2026-09-16**: Committed `749bdb6` "Add exercise plans and homeopathic medicine recommendations" and pushed it to `origin/main`. The user will reboot the Streamlit Cloud app. No new dependencies or secrets are needed.
- **2026-09-16**: The user asked for **less text in medicine lists**, so the AI now outputs only a table: `| Recommendation | Potency | Rate |` (Rate = dose, frequency, duration). Removed the case summary, rationale, instructions, cautions, missing info and changes sections from `medicine_template.md` / `medicine_system.md` and the matching instructions in `ai.recommend_medicines`. The prompt still says not to stop conventional medication, but the output no longer has a cautions section. The app still adds the title, the "Generated…" line and `## Doctor's Input` (for revisions). Real Gemini test: MedicineList4 (3 rows) → revised MedicineList5 (removed the last medicine, Kali bich 30C twice daily for 5 days), table only. Fake tests 17/17. Server restarted.
- **2026-09-16**: Added **homeopathic medicine recommendations**. `drive.py`: `Homeopathic_Medicine` folder, `MEDICINE_LIST_PREFIX`. `ai.py`: `recommend_medicines(name, info, extra, doctor_input, current_list=None, photos=None)`; shared `_system()` / `_case_parts()` helpers; Gemini retry on server errors. `app.py`: `MEDICINE` PlanKind, `screen_medicine`, `generate_and_save_medicines`, shared `read_case()` / `save_next_version()`; patient screen buttons are now 4 rows (Download/Open, Diet/Exercise, Medicines, Update/Back); version dropdown label is now "Version". New prompts `medicine_system.md` / `medicine_template.md` (case summary, ranked medicine table with potency/dose/duration, rationale, instructions, cautions, missing info, changes). Tests: 17/17 medicine tests with a fake Drive (including AI failure, empty revision input, chained revisions, back navigation, downloads), and the earlier 13 edge tests plus the smoke test still pass. Real Gemini + Drive on "yuvraj": MedicineList1 generated; the first revision hit Gemini 503 (the app showed the error, nothing was saved, the input was kept), so I added the retry; the rerun created MedicineList2, which applied every instruction (removed Lycopodium, Kali bichromicum 30C twice daily for 5 days at rank 1, shorter instructions) with a correct Changes section. Restarted the local Streamlit server.
- **2026-09-16**: Testing the exercise-plan feature. Localhost showed `AttributeError: module 'drive' has no attribute 'PLAN_PREFIX'`. The code was fine; the Streamlit server had been started before the edits, and a Streamlit rerun reloads `app.py` but keeps already-imported modules (`drive`, `ai`). **Restart `streamlit run` after changing `drive.py`/`ai.py`/`config.py`.** 13 edge-case tests with a fake Drive passed: a patient created before this feature (no `Exercise_Plan/` folder), an AI failure (error shown, nothing saved), versioning kept separate from nutrition plans, stray files ignored, downloads, and the update/back flows. Real Drive + Gemini run on test patient "yuvraj": pass. Found that Gemini left out "Changes from Previous Plan" on exercise updates (the template said "from Plan 2 onward"), so I reworded the exercise template and made the update instruction in `ai.py` name that section; `ExercisePlan3.md` then included it. Test patient "yuvraj" now has ExercisePlan1–3.
- **2026-09-16**: Added **exercise plans**. `drive.py`: `Exercise_Plan` folder (created on demand for existing patients too), `list_plans`/`save_new_plan` take a file-name prefix. `ai.py`: `generate_plan(..., kind="nutrition"|"exercise")` with `PLAN_PROMPTS`. `app.py`: `PlanKind` (NUTRITION/EXERCISE), new button (patient screen buttons now 3 rows: Download/Open, Diet/Exercise plan, Update/Back), exercise plan section in Open Case Study; `last_plan` is now `(kind, number, text)`. New prompts `exercise_system.md` / `exercise_template.md` (precautions, weekly plan, sample week, progression, stop-and-seek-advice signs). README and PRIVACY updated. Smoke-tested with a fake Drive + mocked AI (create → exercise plan 1 & 2 → diet plan 2 → open case study): pass. Not yet run against a real AI call.
- **2026-09-16**: Switched to **millionairenext01@gmail.com**. New Cloud project `nutritionplan-508807` with a Desktop OAuth client; the consent screen is **In production** (so no test-user list and no 7-day token expiry; sign-in shows the "unverified app" warning, which is expected). Signed in locally and confirmed via `about.get` that the app acts as millionairenext01. **First real end-to-end run succeeded**: patient "yuvraj" created from a photo → `Info.md` (3,990 chars, Gemini `gemini-3.6-flash`) → `Plan1.md`. Regenerated `.streamlit/secrets.toml` with the new token. Remaining for deployment: paste those secrets into the Streamlit app (`patient-case-study.streamlit.app`) and reboot it. Branding home page should become the Streamlit URL; privacy policy stays on GitHub because the app is password-protected.
- **2026-09-16**: **Google account `appstorageruk@gmail.com` was disabled by Google** ("created or used with multiple other accounts"), taking Cloud project `nutritionplan-508717` with it: token refresh now fails with `disabled_client: The OAuth client was disabled`. No patient data lost (the Drive folder was still empty). The Gemini API key still works. Moved `credentials.json` / `token.json` to `disabled_account_backup/` (git-ignored) and deleted the stale `.streamlit/secrets.toml`. Switching to **millionairenext01@gmail.com**: needs a new Cloud project, Drive API, OAuth consent (External/Testing + that address as test user), a new **Desktop** client saved as `credentials.json`, then a fresh local login and a regenerated secrets file. No code changes needed. Streamlit app `patient-case-study.streamlit.app` exists but has no secrets yet, so it shows "APP_PASSWORD is not set".
- **2026-09-15**: Google blocked **Publish app** until the Branding page has an app name, support email, homepage URL and privacy policy URL. Added `PRIVACY.md` (single-user tool; data stays in the owner's Drive; sent to the AI provider; Google API Limited Use statement) and pushed it. Branding URLs: homepage `https://github.com/yuvrajkhelkar01/NutritionPlan`, privacy `https://github.com/yuvrajkhelkar01/NutritionPlan/blob/main/PRIVACY.md`. Both depend on the repo staying **public**; if it's made private, host the policy somewhere else first.
- **2026-09-15**: Started Streamlit Cloud deployment. Pinned `requirements.txt` to the locally tested versions (streamlit 1.63.0, google-api-python-client 2.200.0, google-auth 2.58.0, google-auth-oauthlib 1.4.1, httplib2 0.32.0, google-genai 2.23.0, anthropic 1.6.0, Pillow 12.3.0, python-dotenv 1.2.3), verified with a clean Python 3.12 install, and pushed. The steps on share.streamlit.io are done by the user.
- **2026-09-15**: Pushed commit `3ef3fd0` to GitHub: <https://github.com/yuvrajkhelkar01/NutritionPlan> (branch `main`, remote `origin`). **The repo is public.** A private repo was recommended; the user decides. Push auth came from Windows Git Credential Manager (`/mnt/c/Program Files/Git/mingw64/bin/git-credential-manager-core.exe`), passed with `git -c credential.helper=…` for that command only; no global git config was changed. Next: publish the OAuth app to production, then deploy on share.streamlit.io with `.streamlit/secrets.toml` pasted into Secrets.
- **2026-09-15**: **Prepared for Streamlit Community Cloud** (the user chose to deploy). `config.py` reads each setting from env/.env first, then `st.secrets`. New `GOOGLE_TOKEN_JSON` setting (the contents of token.json): when set, `drive.load_cached_credentials()` builds credentials from it, refreshes in memory only, skips the Login screen and hides Log out. Generated git-ignored `.streamlit/secrets.toml` (APP_PASSWORD, AI_PROVIDER, GEMINI_API_KEY, GEMINI_MODEL, GOOGLE_TOKEN_JSON) to paste into the Cloud Secrets box. `.gitignore` now also covers `credentials*.json` (there's a `credentials_old.json` holding the old Web client secret), `secrets.toml` and `.claude/settings.local.json`. README section 5 has the deploy steps. Tested cloud mode against real Drive with only the secret: pass. `git init` on `main`. No `gh` CLI and no SSH key at the time, so the user pushed via VS Code "Publish to GitHub" (private). (Since 2026-09-16 `gh` is installed; see the changelog.) **Important:** the OAuth app must be switched to "In production"; in Testing mode the refresh token expires after 7 days (token issued 2026-09-15) and the cloud app would lose Drive access.
- **2026-09-15**: Added an **app password gate** (`APP_PASSWORD` in `.env` / Streamlit secrets). It is checked before anything else in every browser session; the app refuses to open while the password is empty; the check uses constant-time `hmac.compare_digest`; after 5 wrong attempts the app locks for 5 minutes, shared across sessions via `st.cache_resource`; the sidebar has a **Lock app** button. Reason: the user plans to make the app reachable from anywhere, and the Google OAuth token is shared by the app, so it does not identify visitors. Deployment discussion: plain "Deploy" on Streamlit Community Cloud is **not** enough, because the local-server OAuth flow doesn't work there and secrets aren't uploaded. Options offered: A) keep it local and reach it through Tailscale (no code changes), or B) cloud deploy (token and keys in secrets, private GitHub repo). The user hasn't chosen yet.
- **2026-09-15**: Project started. Created `Note.md`.
- **2026-09-15**: AI provider set to **Gemini**. The user created `.env` (`AI_PROVIDER=gemini`). `gemini-2.5-flash` returned 404 ("no longer available to new users"), so switched to **`gemini-3.6-flash`** in `.env` and as the default in `config.py`; a live test call with the key succeeded. Installed `google-genai` 2.23.0 and added it to `requirements.txt`. Disabled Gemini automatic function calling (unused; it printed a warning). Open: the real Gemini key is still in `.env.example` and should be removed from there.
- **2026-09-15**: **First real Google login succeeded** (after adding the account as a test user; the earlier block was `403 access_denied`). `token.json` saved with a refresh token; `NutritionPlan/` folder exists in Drive (no patients yet). Still to do: create `.env` and choose the AI provider (the user's Gemini key is currently in `.env.example`; `google-genai` is not installed).
- **2026-09-15**: Login fix. The user first created a *Web* OAuth client (redirect only `localhost:8501`), so it was replaced with a **Desktop app** client. Then Login hung under WSL: `webbrowser`/`gio` can't open a browser ("Operation not supported") and the URL was only printed to stdout. `drive.login(show_link)` now passes the URL to the UI through a custom `webbrowser` controller, and the app shows an **Open Google sign-in** button; the timeout is 180 s. Verified the local redirect round trip and the button rendering.
- **2026-09-15**: Built the full app per the spec: `app.py`, `drive.py`, `ai.py`, `config.py`, `prompts/`, `requirements.txt`, `.env.example`, `.gitignore`, `README.md`. Created `.venv` with dependencies (streamlit 1.63.0, anthropic 1.6.0). Smoke test (AppTest + fake Drive/AI) passed all checks: create, search, plan versioning, update add/replace, downloads, Claude request building.
